def find_closest_elements(numbers: List[float]) -> Tuple[float, float]:
    return tuple(sorted((a, b) for a, b in zip(numbers, numbers[1:]) if isinstance(a, (int, float)) and isinstance(b, (int, float)), key=lambda x: abs(x[0] - x[1]))[0])