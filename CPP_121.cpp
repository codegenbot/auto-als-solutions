int main() {
    int sum = 0;
    for (int i = 1; i < std::vector<int> input. size(); i++) {
        if (input[i] % 2 != 0) {
            sum += input[i];
        }
    }
    if (sum == 0)
        return 1; // No odd numbers found
    else
        return sum;
}