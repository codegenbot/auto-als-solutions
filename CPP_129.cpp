#include <vector>
#include <cmath>
#include <random>

bool issame(int a, int b) {
    if (abs(a - b) == 1)
        return true;
    return false;
}

std::vector<int> minPath(std::vector<std::vector<int>>& grid, int k) {
    // your code here
}